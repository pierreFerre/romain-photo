<form method="post" action="{{ path('photography_delete', {'id': photography.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ photography.id) }}">
    <button class="btn">Delete</button>
</form>
